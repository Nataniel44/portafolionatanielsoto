---
// src/components/ProjectCard.astro
export interface Props {
  title: string;
  description: string;
  image: string;
  slug: string;
  url: string; // Add a new optional URL prop
}

const { title, description, image, slug, url } = Astro.props;

---

<div
class="group block rounded-xl bg-gray-800/50 ring-1 ring-white/10 p-4 hover:bg-gray-800/70 transition view-transition"
>
<a href={`/projects/${slug}/`}>
    <img src={image} alt={title} class="h-48 w-full object-cover rounded-md mb-4" loading="lazy" />
    <h3 class="text-lg font-semibold text-white group-hover:text-indigo-400">{title}</h3>
    <p class="text-sm text-gray-400 mt-1">{description}</p>
  </a>


  <a
  href={`/projects/${slug}/`}
  class="inline-block  mt-4 px-4 py-2 text-sm font-medium text-white bg-indigo-600 rounded-md hover:bg-indigo-700 transition"
  >Ver más</a>

  
</div>
